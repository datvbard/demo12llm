// This is your Prisma schema file
// Phase 01: Empty schema, models added in Phase 02

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
